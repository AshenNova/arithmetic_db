<% if (logRewards)  {%>
<div class="container mt-4">
    <h1> Rewards Log</h1>
    
            <% logRewards.forEach(item => { %>
                <% if (item.points){ %>
                    🔔 <%= item.claimed.toLocaleDateString('en-GB') %> @ <%= item.claimed.toLocaleTimeString('en-US') %>: 👤 <b><%=  item.username %></b> claimed "<%= item.reward %>" with <%= item.points %> points.</p>
               <%  } else { %>
                    🎓 <%= item.claimed.toLocaleDateString('en-GB') %> @ <%= item.claimed.toLocaleTimeString('en-US') %>: 👤 <b><%=  item.username %></b> claimed "<%= item.reward %>"" as a graduation gift.</p>
              <%  } %>
               
            <% }) %>
  </div>
  <% } %>